// import _throttle from 'lodash/throttle';
//
// export function throttle(target, name, descriptor) {
//   if (typeof descriptor.value !== 'undefined') {
//     descriptor.value = _throttle(descriptor.value, 10);
//   }
// }
